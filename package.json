{
  "name": "ross-blocks-h5",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc -b && vite build",
    "build:production": "npm run build && node scripts/prepare-production-tree.mjs",
    "build:baota": "npm run build && node scripts/prepare-baota-release.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "vue-tsc -b",
    "test:engine": "tsx tests/engine.test.ts"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.20"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "~5.9.2",
    "tsx": "^4.20.5",
    "vite": "^7.1.4",
    "vue-tsc": "^3.0.6"
  }
}
